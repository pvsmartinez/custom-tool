# ─────────────────────────────────────────────────────────────
# custom-tool environment variables
# Copy this file to .env and fill in your values.
# NEVER commit .env to git.
# ─────────────────────────────────────────────────────────────

# GitHub Personal Access Token with `copilot` scope
# Generate at: https://github.com/settings/tokens
# Required scopes: copilot (for Copilot API access)
VITE_GITHUB_TOKEN=

# Google OAuth 2.0 credentials (Google Drive backup + Google Slides generation)
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create a project, enable "Google Drive API" and "Google Slides API"
# 3. Create credentials → OAuth 2.0 Client ID → choose "Desktop app"
# 4. Copy Client ID and Client Secret below
# Scopes used: drive.file (only files the app creates) + presentations
VITE_GOOGLE_CLIENT_ID=
VITE_GOOGLE_CLIENT_SECRET=

# Anthropic API key (fallback / future model support)
# Generate at: https://console.anthropic.com/
VITE_ANTHROPIC_API_KEY=

# ─────────────────────────────────────────────────────────────────────────────
# .env.local  —  Copy this file to .env.local and fill in your values.
# .env.local is in .gitignore and will NEVER be committed.
# ─────────────────────────────────────────────────────────────────────────────

# ── Apple Developer Team ──────────────────────────────────────────────────────
# Your 10-character Team ID.
# Find it at: https://developer.apple.com/account → Membership Details
APPLE_DEVELOPMENT_TEAM=XXXXXXXXXX

# ── App Store Connect API Key (for TestFlight uploads) ────────────────────────
# Create a key at:
#   https://appstoreconnect.apple.com/access/integrations/api
#   → Keys tab → "+" button → name it "custom-tool CI", role = "Developer"
#
# Copy Key ID and Issuer ID from that page, then download AuthKey_XXXXX.p8
# Place the .p8 file here (don't rename it):
#   mkdir -p ~/.private_keys
#   mv ~/Downloads/AuthKey_*.p8 ~/.private_keys/
#
APPLE_API_KEY_ID=XXXXXXXXXX
APPLE_API_ISSUER_ID=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
